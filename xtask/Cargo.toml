[package]
name = "xtask"
version = "0.2.0"
edition = "2024"
description = "Repository health command (cargo xtask guard)"
license = "MIT"
publish = false

[package.metadata.dist]
dist = false

[dependencies]
serde_json = "1"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
