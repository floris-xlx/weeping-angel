schema = "weeping-angel/framework-pack/v1"

# Pack-only annotations. Canonical controls/tests live in catalog/canonical/v1/.
# Do not declare competing sliver control or test libraries here.

[pack]
library = "catalog/canonical/v1"
note = "ISO 27001:2022 is a framework projection over the canonical catalog."
